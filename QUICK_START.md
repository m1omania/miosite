# Быстрый запуск локально

## ✅ Зависимости установлены

## 🚀 Запуск (в двух терминалах)

### Терминал 1 - Backend:
```bash
cd backend
npm run dev
```

Backend запустится на `http://localhost:4001`

### Терминал 2 - Frontend:
```bash
cd frontend
npm run dev
```

Frontend запустится на `http://localhost:4000`

## 🌐 Откройте в браузере:
- **Frontend**: http://localhost:4000
- **Backend API**: http://localhost:4001

## ⚙️ Настройка портов (если нужно)

Если порты 4000/4001 тоже заняты, можно изменить:

### Вариант 1: Через переменные окружения

**Backend** (`backend/.env`):
```env
PORT=5001
CORS_ORIGIN=http://localhost:5000
```

**Frontend** (`frontend/.env`):
```env
NEXT_PUBLIC_API_URL=http://localhost:5001
```

И запустите frontend с портом:
```bash
cd frontend
PORT=5000 npm run dev:custom
```

### Вариант 2: Через скрипты

Измените порты в:
- `backend/src/index.ts` (строка 15)
- `frontend/package.json` (скрипт dev)

## ⚙️ Настройка OpenAI (опционально)

Если нужен визуальный анализ через OpenAI:
1. Откройте `backend/.env`
2. Добавьте: `OPENAI_API_KEY=sk-your-key-here`

Без OpenAI ключа проект тоже работает - будет базовый анализ (HTML/CSS парсинг).

## 🧪 Тест

Попробуйте проанализировать любой сайт, например:
- example.com
- google.com
- github.com
