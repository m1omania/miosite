# Инструкция по деплою

## 🚀 Быстрый деплой

### 1. Backend на Render

1. Зайдите на https://render.com и создайте аккаунт
2. Нажмите "New +" → "Web Service"
3. Подключите репозиторий GitHub: `m1omania/dr`
4. Заполните настройки:
   - **Name:** `ux-audit-backend`
   - **Region:** выберите ближайший
   - **Branch:** `main`
   - **Root Directory:** оставьте пустым (или `backend` если Render требует)
   - **Environment:** `Node`
   - **Build Command:** `cd backend && npm install && npm run build`
   - **Start Command:** `cd backend && npm start`
   - **Plan:** Free (или выберите нужный)

5. В разделе "Environment Variables" добавьте:
   ```
   NODE_ENV=production
   PORT=10000
   DATABASE_PATH=/opt/render/project/src/database.sqlite
   HUGGINGFACE_API_KEY=hf_ваш_токен_здесь
   CORS_ORIGIN=https://your-app.vercel.app
   ```
   (OPENAI_API_KEY опционально, если хотите использовать OpenAI)

6. Нажмите "Create Web Service"
7. Дождитесь деплоя (5-10 минут)
8. Скопируйте URL сервиса (например: `https://ux-audit-backend.onrender.com`)

### 2. Frontend на Vercel

1. Зайдите на https://vercel.com и создайте аккаунт
2. Нажмите "Add New..." → "Project"
3. Импортируйте репозиторий: `m1omania/dr`
4. Настройки проекта:
   - **Framework Preset:** Next.js (автоматически определится)
   - **Root Directory:** `frontend` ⚠️ ВАЖНО!
   - **Build Command:** `npm run build` (автоматически)
   - **Output Directory:** `.next` (автоматически)

5. В разделе "Environment Variables" добавьте:
   ```
   NEXT_PUBLIC_API_URL=https://ux-audit-backend.onrender.com
   ```
   (используйте URL вашего backend с Render)

6. Нажмите "Deploy"
7. Дождитесь деплоя (2-3 минуты)
8. Скопируйте URL вашего приложения (например: `https://your-app.vercel.app`)

### 3. Обновление CORS

После деплоя frontend, вернитесь в Render и обновите переменную:
```
CORS_ORIGIN=https://your-app.vercel.app
```
(используйте реальный URL вашего frontend)

Перезапустите сервис в Render (или подождите автоматического перезапуска).

## ✅ Проверка

1. Откройте URL вашего frontend на Vercel
2. Попробуйте проанализировать сайт (например: `example.com`)
3. Если всё работает - деплой успешен! 🎉

## ⚠️ Важные замечания

### Render Free Plan:
- Сервис "засыпает" после 15 минут бездействия
- Первый запрос после пробуждения может занять 30-60 секунд
- Для продакшена рекомендуется платный план

### Vercel:
- Автоматический деплой при push в `main`
- Бесплатный план достаточен для MVP
- CDN и оптимизация включены по умолчанию

### Переменные окружения:
- **HUGGINGFACE_API_KEY** - обязательно для визуального анализа
- **OPENAI_API_KEY** - опционально, для более качественного анализа
- **CORS_ORIGIN** - должен совпадать с URL frontend

## 🔧 Troubleshooting

### Backend не запускается:
- Проверьте логи в Render Dashboard
- Убедитесь, что все зависимости установлены
- Проверьте переменные окружения

### Frontend не подключается к backend:
- Проверьте `NEXT_PUBLIC_API_URL` в Vercel
- Проверьте `CORS_ORIGIN` в Render
- Убедитесь, что backend запущен (проверьте `/health` endpoint)

### Медленная работа:
- Render free plan может быть медленным при пробуждении
- Рассмотрите upgrade на платный план для продакшена

