# Пошаговая инструкция: Исправление ошибки 503

## Шаг 1: Настроить перенаправление портов в панели Jino.ru

1. Откройте панель управления Jino.ru
2. Найдите раздел **"Проксирование портов 80/443"**
3. В поле **"Порт по умолчанию"** введите: `4001`
4. Нажмите кнопку **"Сохранить"**

✅ После этого все запросы к `http://53893873b619.vps.myjino.ru` будут перенаправляться на порт 4001 (ваш backend).

---

## Шаг 2: Проверить, что backend слушает на всех интерфейсах

Подключитесь к серверу и проверьте:

```bash
ssh -p 49376 root@53893873b619.vps.myjino.ru
```

Проверьте статус backend:
```bash
systemctl status ux-audit-backend
```

Проверьте, на каких интерфейсах слушает backend:
```bash
netstat -tulpn | grep 4001
```

**Должно быть:** `tcp 0 0 0.0.0.0:4001` (не только `:::4001`)

Если видите только `:::4001`, обновите backend:
```bash
cd /opt/ux-audit/backend
git pull origin main
npm run build
systemctl restart ux-audit-backend
```

Проверьте снова:
```bash
netstat -tulpn | grep 4001
```

---

## Шаг 3: Проверить доступность backend

С сервера:
```bash
curl http://localhost:4001/health
```

Должно вернуть: `{"status":"ok","timestamp":"..."}`

---

## Шаг 4: Проверить доступность через порт 80

После настройки перенаправления в панели Jino.ru, проверьте:

```bash
# С вашего компьютера
curl http://53893873b619.vps.myjino.ru/health
```

Должно вернуть: `{"status":"ok","timestamp":"..."}`

Если не работает, подождите 1-2 минуты (настройки могут применяться с задержкой).

---

## Шаг 5: Обновить переменную окружения на Vercel

1. Откройте Vercel Dashboard: https://vercel.com
2. Выберите ваш проект
3. Перейдите в **Settings** → **Environment Variables**
4. Найдите переменную `API_URL` (или создайте новую)
5. Установите значение: `http://53893873b619.vps.myjino.ru`
   - ⚠️ **БЕЗ** `:4001` в конце!
6. Нажмите **Save**

---

## Шаг 6: Передеплоить frontend на Vercel

1. В Vercel Dashboard перейдите в **Deployments**
2. Найдите последний деплой
3. Нажмите **"..."** (три точки) → **Redeploy**
4. Или просто сделайте новый commit и push (автоматический деплой)

---

## Шаг 7: Проверить работу

1. Откройте ваш frontend на Vercel
2. Попробуйте проанализировать сайт
3. Если ошибка 503 исчезла - ✅ готово!

---

## Если не работает

### Проверьте логи Vercel:
1. Vercel Dashboard → **Deployments** → ваш деплой
2. **Functions** → **api/audit** → **Logs**

Ищите ошибки:
- `ECONNREFUSED` - backend недоступен
- `fetch failed` - проблема с подключением
- `timeout` - backend слишком медленный

### Проверьте логи backend на сервере:
```bash
ssh -p 49376 root@53893873b619.vps.myjino.ru
journalctl -u ux-audit-backend -f
```

### Проверьте доступность:
```bash
curl http://53893873b619.vps.myjino.ru/health
curl http://53893873b619.vps.myjino.ru/api/audit
```

---

## Готово! 🎉

После выполнения всех шагов ошибка 503 должна исчезнуть, и Vercel сможет подключаться к backend через стандартные порты 80/443.

