# Настройка Hugging Face Inference API

## 🚀 Быстрая настройка

### 1. Получите API ключ

1. Зарегистрируйтесь на https://huggingface.co
2. Перейдите в Settings → Access Tokens
3. Создайте новый токен (тип: Read)
4. Скопируйте токен (начинается с `hf_...`)

### 2. Добавьте в `.env`

Откройте `backend/.env` и добавьте:

```env
HUGGINGFACE_API_KEY=hf_ваш_токен_здесь
```

### 3. Перезапустите backend

```bash
cd backend
npm run dev
```

## ✅ Как это работает

1. **BLIP модель** анализирует скриншот и создаёт описание
2. **Анализ описания** находит проблемы по ключевым словам:
   - Перегруженный дизайн
   - Мелкие элементы
   - Проблемы с контрастом
   - Наличие структуры (header, navigation, buttons)

## 📊 Что анализируется

- Композиция и структура
- Размеры элементов
- Цветовая схема
- Наличие элементов навигации
- Призывы к действию

## ⚙️ Приоритет использования

Система использует API в следующем порядке:

1. **OpenAI** (если есть ключ) - самый качественный анализ
2. **Hugging Face** (если есть ключ) - бесплатный анализ
3. **Fallback** - базовый анализ без визуального

## 💡 Бесплатный лимит

- **1000 запросов/месяц** бесплатно
- Достаточно для MVP и тестирования
- После лимита API вернёт ошибку, но система продолжит работать с базовым анализом

## 🔧 Troubleshooting

### Модель загружается долго

При первом запросе модель может загружаться 10-30 секунд. Система автоматически ждёт и повторяет запрос.

### Ошибка "Model is currently loading"

Это нормально для первого запроса. Система автоматически ждёт и повторяет.

### Ошибка авторизации

Проверьте что токен правильный и начинается с `hf_`.


