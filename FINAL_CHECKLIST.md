# Финальный чеклист после снижения нагрузки

## ✅ Нагрузка снизилась!

CPU и RAM должны быть в нормальных пределах после добавления `USE_SINGLE_PROCESS=true`.

## Финальная проверка

### 1. Проверить работу backend

```bash
# Health check
curl http://53893873b619.vps.myjino.ru/health

# Тестовый запрос
curl -X POST http://53893873b619.vps.myjino.ru/api/audit \
  -H "Content-Type: application/json" \
  -d '{"url":"https://example.com"}'
```

### 2. Проверить переменную окружения на Vercel

В Vercel Dashboard:
- Settings → Environment Variables
- Проверьте `API_URL`:
  - Должно быть: `http://53893873b619.vps.myjino.ru` (без `:4001`)
  - Если указано с `:4001`, удалите `:4001`

### 3. Передеплоить frontend на Vercel

В Vercel Dashboard:
- Deployments → последний деплой → "..." → Redeploy

Или сделайте новый commit (автоматический деплой).

### 4. Проверить работу frontend

1. Откройте frontend на Vercel
2. Попробуйте проанализировать сайт
3. Ошибки 500/502 должны исчезнуть

## Опционально: Обновить CORS_ORIGIN

В `.env` на сервере можно обновить `CORS_ORIGIN`:

```bash
cd /opt/ux-audit/backend
# Удалить старую строку с :4001
sed -i 's|http://53893873b619.vps.myjino.ru:4001|http://53893873b619.vps.myjino.ru|g' .env
# Или добавить новую строку без :4001
echo 'CORS_ORIGIN=https://dr-henna.vercel.app,http://53893873b619.vps.myjino.ru' >> .env
```

Затем перезапустить:
```bash
systemctl restart ux-audit-backend
```

## Текущий статус

- ✅ USE_SINGLE_PROCESS=true добавлен
- ✅ Backend перезапущен
- ✅ Нагрузка CPU и RAM снизилась
- ⏳ Нужно проверить работу frontend на Vercel
- ⏳ Нужно проверить API_URL на Vercel (без :4001)

## Ожидаемый результат

После всех проверок:
- ✅ Backend работает стабильно
- ✅ Нагрузка CPU и RAM в норме
- ✅ Frontend на Vercel может подключиться к backend
- ✅ Ошибки 500/502 исчезли
- ✅ Анализ сайтов работает корректно

## Если проблемы остались

### Проверить логи Vercel
В Vercel Dashboard:
- Deployments → ваш деплой → Functions → api/audit → Logs

Ищите:
- Таймауты
- Ошибки подключения
- Детали ошибок

### Проверить логи backend
```bash
journalctl -u ux-audit-backend -n 50 --no-pager
```

Ищите:
- Ошибки запуска браузера
- Ошибки Chrome
- Ошибки AI API

## Готово! 🎉

После всех проверок система должна работать корректно!

